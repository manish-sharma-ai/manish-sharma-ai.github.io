---
import { internalHref } from "@utils/urls";

interface Props {
  label: string;
  href: string;
  description: string;
}

const { label, href, description } = Astro.props;
const normalizedHref = internalHref(href);
const isExternal = normalizedHref.startsWith("http");
---

<article class="resource-card">
  <a href={normalizedHref} target={isExternal ? "_blank" : undefined} rel={isExternal ? "noreferrer" : undefined}>
    <h3>{label}</h3>
    <p>{description}</p>
    <span class="resource-card__action">Open resource <span aria-hidden="true">→</span></span>
  </a>
</article>
