---
import SourceLinks from "@components/SourceLinks.astro";

interface LinkItem {
  label: string;
  href?: string | null;
  status?: string;
  description?: string;
}

interface Props {
  links: LinkItem[];
  sourceNotes?: LinkItem[];
}

const { links, sourceNotes = [] } = Astro.props;

const defaultSourceNotes: LinkItem[] = [
  {
    label: "Further reading to verify: review literature on in-situ monitoring in metal additive manufacturing.",
    href: null,
    status: "Working-draft source note"
  },
  {
    label: "Further reading to verify: ISO/ASTM additive manufacturing terminology.",
    href: null,
    status: "Working-draft source note"
  },
  {
    label: "Further reading to verify: public Exafuse LMD case study.",
    href: null,
    status: "Working-draft source note"
  }
];

const resolvedSourceNotes = sourceNotes.length > 0 ? sourceNotes : defaultSourceNotes;
---

<section class="mt-12 grid gap-8 lg:grid-cols-2">
  <div>
    <h2 class="text-2xl font-black text-white">Related pages</h2>
    <div class="mt-5">
      <SourceLinks links={links} />
    </div>
  </div>
  <div>
    <h2 class="text-2xl font-black text-white">Related source notes</h2>
    <p class="mt-3 text-sm leading-6 text-slate-400">
      These are working-draft source notes for future citation links. They are not citations and should not be treated as verified references until a real source URL is added.
    </p>
    <div class="mt-5">
      <SourceLinks links={resolvedSourceNotes} />
    </div>
  </div>
</section>
