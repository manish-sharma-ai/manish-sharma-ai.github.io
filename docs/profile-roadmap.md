# Profile Roadmap

These profile targets are not public-facing facts until real, verified URLs exist.

Planned profiles:

- ORCID
- Zenodo
- Hugging Face
- Google Scholar
- ResearchGate

Rules:

- Do not show planned profiles on public identity, links, press-kit, or public-profile pages.
- Do not include planned profiles in JSON-LD `sameAs`.
- Do not create placeholder URLs.
- Add a profile to `src/data/profiles.ts` public surfaces only after the real URL is verified.
